pub struct RenderEngine {

}

impl RenderEngine {
	pub fn make_window(&self, title: &str, width: u32, height: u32, sdl: &sdl3::Sdl) {
		println!("Creating window with title: {}, width: {}, height: {}", title, width, height);

		
	}
}